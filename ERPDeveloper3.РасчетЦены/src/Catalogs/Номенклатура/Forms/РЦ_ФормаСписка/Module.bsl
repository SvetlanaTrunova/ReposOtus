
&НаКлиенте
Процедура СписокПриАктивизацииСтроки(Элемент)
	Заголовок = СформироватьЗаголовокФормы(Элемент.ТекущаяСтрока);
КонецПроцедуры  

&НаСервереБезКонтекста
Функция СформироватьЗаголовокФормы(Номенклатура)
	Если Номенклатура.ЭтоГруппа Тогда
		Возврат "";	
	КонецЕсли;
	
	Цена = РегистрыСведений.УчетЦенНоменклатуры.ПолучитьЦенуНоменклатуры(Номенклатура);
	
	Возврат СтрШаблон("%1. Цена = %2", Номенклатура, Цена);
КонецФункции // СформироватьЗаголовокФормы

